# Topicos Importantes

- O que é o Node.js?
-O que é NPM? (Node packge manager)
 R: Gerenciador de pacotes padrão
 - O que é o Express.js?
 R:É um Framework para desenvolver APIs backend
 -Como mudar o formato de importação de "require" (padrão commonjs) para module?
 R: packge.json -> "type": "module"
 -O que é HTTP?
 -Quais os métodos de requisição HTTP e para que serve cada um deles?
R: GET(pega os dados),PUT(atualiza todos os dados),PATCH(atualiza algo especifico no banco de dados),POST(cadastro no banco de dados),DELETE(remover algum dando no banco de dados)
-Para que serve o --watch ao rodar o servidor?
- Para que serve o Router do Express?
R: Separar um conjunto de rotas similares (end points) em cada arquivo